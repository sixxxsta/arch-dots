# Workspace Configuration
# Configure workspace assignments per monitor if needed
# Format: workspace=ID,monitor:NAME,default:true

# Default workspaces (will use available monitors)
# workspace=1,default:true
