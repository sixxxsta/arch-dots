// Shell Switcher - Startup Configuration
// This file is managed by shell-switch - manual edits will be overwritten
// Current shell: {{SHELL_NAME}}

spawn-at-startup "bash" "-lc" "{{LAUNCH_CMD}} || (command -v qs >/dev/null 2>&1 && qs -c noctalia-shell) || (command -v quickshell >/dev/null 2>&1 && quickshell -c noctalia-shell) || (command -v dms >/dev/null 2>&1 && dms run)"
